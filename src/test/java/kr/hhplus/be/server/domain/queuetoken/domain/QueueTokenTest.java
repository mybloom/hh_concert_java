package kr.hhplus.be.server.domain.queuetoken.domain;

import static org.assertj.core.api.Assertions.assertThat;

import kr.hhplus.be.server.domain.user.domain.User;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class QueueTokenTest {

  /*  @DisplayName("사용자의 대기열토큰을 UUID로 생성하여 반환한다")
    @Test
    void createTokenUuid() {
    	//given
        User user = new User();
        QueueToken queueToken = new QueueToken();

        //when
        String uuid = queueToken.createUuid(user);

    	//then
        assertThat(uuid).isNotNull();
    }*/
}